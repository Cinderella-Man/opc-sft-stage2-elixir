Given the following JSON:

```json
<PASTE JSON HERE>
````

Analyze the `instruction` and `elixir_code` fields.

Respond in two sections:

1. Reformatted Elixir Code

* Present the `elixir_code` value properly formatted using idiomatic Elixir style.
* Do not change the logic.

2. Issues and Improvements

* List only problems, non-idiomatic patterns, or incorrect aspects.
* Do NOT include any positive feedback.
* Cover both the `instruction` and the `elixir_code`.
* For each issue, include a brief explanation and (if possible) a suggested fix.
