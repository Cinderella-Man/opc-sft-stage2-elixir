# Build + deps
/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
tunex-*.tar

# Run-scoped + cached state (regenerable / large)
/var/

# Secrets (real values live here; commit secrets.dummy.exs only)
/config/secrets.exs

# Datasets
*.parquet

# v1 generated artifacts (v1 is reference-only; keep source, ignore build/output)
/v1/_build/
/v1/deps/
/v1/.elixir_ls/
/v1/*.parquet
/v1/tunex_workspace*/
/v1/output_v1/
/v1/elixir_sft_*.jsonl
/v1/config/secrets.exs
